require 'spec_helper'
require 'apparatus/mode'

include Apparatus

=begin
  a Mode has in ActionBuilder input channel
  the channel is passed to a mode's 8 Pages
  the channel creates triggers for the scene launch cells
    the scene launch cells cause the active page to change
    the 
=end

describe Mode do
end
